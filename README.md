# Paragon School Frontend

Modern school website frontend built with React, TypeScript, Vite and Tailwind CSS.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_BASE_URL` to the Laravel API URL. Production builds are created with `npm run build`.
